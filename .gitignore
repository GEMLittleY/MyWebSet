# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# generated card / deck data (derived from external sources, regenerable)
/data/*
# but keep the curated hsguru meta snapshot — it's the seed input
!/data/meta-candidates.json
# and the collectible card index — required at build time, regenerated by
# scripts/fetch-cards-index.ts
!/data/cards-index.json
!/data/cards-collectible.json
